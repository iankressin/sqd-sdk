# @sqd-sdk/typeorm-sink

This package provides TypeORM based database access to squid mapping handlers.

It includes TypeORM configuration functionality (previously in @sqd-sdk/typeorm-config) for configuring TypeORM connections using environment variables or code-based configuration.
